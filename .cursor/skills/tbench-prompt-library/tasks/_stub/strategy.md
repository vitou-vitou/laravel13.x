# Stub task pack

This task is registered in [catalog.yaml](../../catalog.yaml) but no trial has been imported yet.

## Until import-trial.md is done

1. Apply [harness/default.md](../../harness/default.md) only.
2. Open [tbench.ai waffle](https://www.tbench.ai/?view=waffle) → task row → white cell → Hub Trajectory.
3. Distill ~40 lines into `tasks/{slug}/strategy.md` and replace this stub pointer in catalog.

## Goal block template

```text
Goal: (user's real ask)
TB analog: {slug} — {one line from meta.yaml summary}
Done when: (verifier / tests)
```
