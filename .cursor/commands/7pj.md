---
name: /7pj
id: 7pj
category: Workflow
description: Short Direct Book wave — all 7 lines or one product; quote list+pdf+view+create+edit then policy wire
---

# /7pj

Load skills **now**: `7pj-wave` + `spec-kit-openspec-superpowers` + `impeccable`.

Playbook: `spec-kit-openspec-superpowers/references/db-journey.md`

**Input** (everything after `/7pj`):

```text
$ARGUMENTS
```

## Expand (do not ask the user to retype waves)

Default product = **all 7 lines** (Marine `0189`+`0206`, `0191`–`0196`). Named product / SM code = that line only.

Default one wave unless `w2`/`w3` or another `do:`:

```text
——wave N:——
task1: 7pj > quote only list + pdf + view + create + edit > read and do: {work}  ref:{url}
task2: 7pj > policy > wire task 1
```

Print the matrix (one line). Then run. No Playwright. No commit unless asked.

Examples: `.cursor/skills/7pj-wave/examples.md`
