---
name: /super-spec-force-openspec
id: super-spec-force-openspec
category: Workflow
description: Force OpenSpec mode for spec-first workflow (writes openspec to .spec-mode)
---

Run `/super-spec` with variant `force-openspec`.

1. Write `openspec` to project-root `.spec-mode`
2. Read and follow `.cursor/skills/spec-kit-openspec-superpowers/SKILL.md`
3. Continue the full spec-first pipeline in **OpenSpec** mode

Use OpenSpec project commands when implementing: `/opsx-explore`, `/opsx-propose`, `/opsx-apply`, `/opsx-archive`.

If the user included a task description after this command, use it as the change to spec — do not write code until G1 confirmation.
