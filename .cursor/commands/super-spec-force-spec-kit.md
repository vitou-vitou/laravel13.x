---
name: /super-spec-force-spec-kit
id: super-spec-force-spec-kit
category: Workflow
description: Force Spec-Kit mode for spec-first workflow (writes spec-kit to .spec-mode)
---

Run `/super-spec` with variant `force-spec-kit`.

1. Write `spec-kit` to project-root `.spec-mode`
2. Read and follow `.cursor/skills/spec-kit-openspec-superpowers/SKILL.md`
3. Continue the full spec-first pipeline in **Spec-Kit** mode

Follow `references/spec-kit-workflow.md` in the skill directory for Spec-Kit commands.

If the user included a task description after this command, use it as the feature to spec — do not write code until G1 confirmation.
