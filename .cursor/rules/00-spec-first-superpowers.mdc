---
description: "Spec-First 守门：每会话按任务走 spec-kit-openspec-superpowers"
globs: "*"
alwaysApply: true
---

## Spec-First + Superpowers（每会话）

**触发**：识别到编码任务（feature / bugfix / refactor / config）→ 先读 `.cursor/skills/spec-kit-openspec-superpowers/SKILL.md`，再行动。

**模式**：OpenSpec（本仓库有 `openspec/`）。根目录无 `.spec-mode` 时写入单行 `openspec`。

### 任务分级（实现前必做）

| 级别 | 场景 | 流程 |
|------|------|------|
| **Quick** | 单文件 bugfix / typo / config | 简化 propose → TDD → 实现 |
| **Standard** | 单功能、范围清晰 | 全阶段，G1–G4 |
| **Thorough** | 多模块 / 架构决策 | 全阶段 + review |

### 闸门

- **G1**：无用户确认的 spec 不写代码。
- 用户说 "skip spec" → 礼貌拒绝，引导 `/super-spec`。
- 已有确认 spec → 按 spec 实现。

### Phase 0 — 会话恢复

- 检查 `openspec/changes/*/task_plan.md`、`progress.md`、`findings.md`。
- 存在则读规划文件，从最后 checkpoint 续作。

### 活跃变更（PL Direct Book）

继续 Property Liability Direct Book / 报价切片工作时，默认上下文：

`openspec/changes/phase-ii-quotation-slice-only/`

### 命令

- `/super-spec` — 启动完整规范流程
- 本规则为守门员；细节以 Skill 为准

### 项目策略

还须遵守 `.cursor/skills/spec-kit-openspec-superpowers/pgi-core-policy.md`（OpenSpec 默认、PL 七产品范围、勿混用 SDD 层）。

**Simple code + voice:** `.cursor/rules/04-simple-code-voice.mdc` · skill `references/simple-code-voice.md`（短名、小函数、白话）。
