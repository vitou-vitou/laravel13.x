---
description: "Spec-First 守门：每会话默认走 spec-kit-openspec-superpowers（99% 默认栈）"
globs: "*"
alwaysApply: true
---

## Spec-First + Superpowers（每会话 · 默认栈）

**Default stack (99% of work):** skill  
`.cursor/skills/spec-kit-openspec-superpowers/SKILL.md`  
(+ `pgi-core-policy.md` · `references/claude-senior-listener.md`)

**Boot (every session, before coding):**

1. Treat `/spec-kit-openspec-superpowers` as **already on** — do not wait for user to attach skill.
2. Read skill `SKILL.md` + `pgi-core-policy.md` when starting any feature / bugfix / refactor / config.
3. Mode = OpenSpec (`.spec-mode` = `openspec`). Missing file → write `openspec`.
4. Claude Senior listener ON + agent-browser when UI needed.
5. Caveman voice ON (`caveman-mode.mdc`).

### 任务分级（实现前必做）

| 级别 | 场景 | 流程 |
|------|------|------|
| **Quick** | 单文件 bugfix / typo / config | 简化 propose → TDD → 实现 |
| **Standard** | 单功能、范围清晰 | 全阶段，G1–G4 |
| **Thorough** | 多模块 / 架构决策 | 全阶段 + review |

### 闸门

- **G1**：无用户确认的 spec 不写代码。
- 用户说 "skip spec" → 礼貌拒绝，引导 `/super-spec`。
- 已有确认 spec → 按 spec 实现。

### Phase 0 — 会话恢复

- 检查 `openspec/changes/*/task_plan.md`、`progress.md`、`findings.md`。
- 存在则读规划文件，从最后 checkpoint 续作。

### 活跃变更（PL Direct Book）

`openspec/changes/phase-ii-quotation-slice-only/`（quotation）  
Also check `docs/SESSION_STATE.md` for current active change.

### 命令

- `/super-spec` — 完整规范流程（same stack）
- `/spec-kit-openspec-superpowers` — explicit reinforce (already default)
- Opt out of whole stack only if user says `no super-spec` / `plain agent`

### 项目策略

`.cursor/skills/spec-kit-openspec-superpowers/pgi-core-policy.md`  
**Simple code:** `.cursor/rules/04-simple-code-voice.mdc`
