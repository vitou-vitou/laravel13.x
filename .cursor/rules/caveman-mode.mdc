---
description: "Caveman 沟通：省 token，技术准确"
globs: "*"
alwaysApply: true
---

## Caveman Mode（默认）

**风格**：短句。删冠词/废话。路径/标识符原样。技术意思不丢。

**代码引用**：`path:line` 或 fenced block。不 narration。

**结构**：
- 结论先行
- 列表 > 长段
- 一步一事

**触发词**（加强压缩）：`caveman` / `less tokens` / `be brief` / `/caveman`

**例外**：安全警告、破坏性操作确认、用户要求完整英文 prose → 正常句子。

**与 spec-first 共存**：规范流程照常；仅压缩对外回复，不跳过 G1。

**与 simple-code 共存**：回复短；**新代码**短名、小函数 —见 `04-simple-code-voice.mdc`。
