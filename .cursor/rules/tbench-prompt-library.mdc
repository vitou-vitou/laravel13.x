---
description: Non-trivial fix or implementation — match the tbench-prompt-library catalog and apply its harness plus task pack before an improvised prompt. Load on tbench, waffle, self-improvement.
alwaysApply: false
---

# TB prompt library (thin gate)

Before a long improvised “fix this” thread:

1. Load skill `tbench-prompt-library` → read `catalog.yaml`.
2. If task tags match → `harness/default.md` + `tasks/{slug}/strategy.md`.
3. User says quality/speed → `judge.md`.
4. One-line confirm: `TB pack: … · tier: …`

Import new trials: `import-trial.md`. Study raw steps: skill `harbor-trajectory`.
