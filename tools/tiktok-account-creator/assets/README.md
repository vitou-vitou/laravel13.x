# Post video assets

Place a short `.mp4` here for `post` / `pipeline` (e.g. `sample.mp4`).

```bash
python cli.py post --video assets/sample.mp4 --caption "Hello" --index 0
```

TikTok requires a real video file; this folder is not committed with media by default.
