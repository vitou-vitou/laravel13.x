; Dev-only PHP overrides. Loaded AFTER zz-app.ini (filename sorts later) so it
; wins. Mounted by docker-compose.override.yml — never baked into the image.
; Lets edited PHP files take effect immediately (opcache revalidates).
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
display_errors = On
