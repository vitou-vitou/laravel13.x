# Next session — Week Ship Atlas

**URL:** http://127.0.0.1:8000 (or herd link `week-ship-atlas.test`)

**Done:** Mon–Sun board, note CRUD, seed, Feature tests, Vite build.

**Nice next:**
1. `herd link week-ship-atlas --update-env`
2. Add Fri “publish draft” textarea → markdown export
3. Multi-week filter
4. Optional login (Breeze) if sharing
