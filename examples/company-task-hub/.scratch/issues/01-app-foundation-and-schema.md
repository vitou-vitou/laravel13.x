# 01: Application Foundation and Database Schema

**What to build:** Scaffold the Laravel 13 application skeleton for Company Task Hub with SQLite support, Pest test harness, Enums (Priority, TaskStatus, IssueSeverity), and migrations for `company_tasks`, `daily_issue_tasks`, and `customer_tasks`.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Laravel 13 baseline structure configured in `examples/company-task-hub`
- [ ] Enum classes for `TaskPriority`, `TaskStatus`, and `IssueSeverity` created
- [ ] Migrations for `company_tasks`, `daily_issue_tasks`, and `customer_tasks` created and runnable
- [ ] Baseline Pest feature tests verify schema and model instantiation
