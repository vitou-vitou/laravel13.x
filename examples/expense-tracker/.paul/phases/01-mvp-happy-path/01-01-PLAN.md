---
phase: 01-mvp-happy-path
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/main.js
  - src/style.css
  - src/storage.js
  - src/ui.js
  - src/chart.js
  - README.md
autonomous: true
description: "Scaffold Vite app; log categorized expenses; show this month’s chart totals"
type: Plan
about: "expense-tracker"
---

<objective>
## Goal
Ship MVP happy path: add an expense with a category, persist it, and see this month’s totals on a chart — no auth.

## Purpose
Proves core value (track expenses + spending patterns) with the smallest greenfield stack: Vite + vanilla JS + localStorage.

## Output
Runnable Vite app under `examples/expense-tracker` with expense form, category select, monthly chart, and README how to run.
</objective>

<context>
## Project Context
@examples/expense-tracker/.paul/PROJECT.md
@examples/expense-tracker/.paul/ROADMAP.md
@examples/expense-tracker/.paul/STATE.md

## Stack decision (this plan)
- Vite + vanilla JS
- Persist in localStorage (no backend, no login)
- Chart: lightweight (Chart.js via CDN or npm — prefer one small chart lib)

## Source Files
(None yet — greenfield)
</context>

<!-- No SPECIAL-FLOWS.md - skills section omitted -->

<acceptance_criteria>

## AC-1: Add categorized expense
```gherkin
Given the app is open with an empty or existing list
When I enter amount, pick a category, and submit
Then the expense appears in the list and survives a page reload
```

## AC-2: Monthly chart totals
```gherkin
Given one or more expenses dated in the current month
When I view the monthly chart
Then I see totals for this month broken down by category (or total + category segments)
```

## AC-3: No auth required
```gherkin
Given a fresh browser with no account
When I open the app
Then I can use all MVP features without login or signup
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Scaffold Vite vanilla app</name>
  <files>package.json, vite.config.js, index.html, src/main.js, src/style.css, README.md</files>
  <action>
    Create a Vite + vanilla JS project in `examples/expense-tracker` (keep `.paul/` intact).
    - `npm create vite` style layout: index.html, src/main.js, src/style.css
    - package.json scripts: `dev`, `build`, `preview`
    - README: how to `npm i` and `npm run dev`
    - Do NOT add auth, backend, or Laravel
    - Do NOT delete or overwrite `.paul/`
  </action>
  <verify>`npm install` and `npm run build` succeed in examples/expense-tracker</verify>
  <done>AC-3 foundation: app loads with no auth; scaffold ready for features</done>
</task>

<task type="auto">
  <name>Expense form + categories + localStorage</name>
  <files>src/storage.js, src/ui.js, src/main.js, index.html, src/style.css</files>
  <action>
    Implement:
    - Form: amount (number), category (select with sensible defaults: Food, Transport, Housing, Utilities, Other), optional note, date defaulting to today
    - List of saved expenses
    - Persist array in localStorage under a clear key (e.g. `expense-tracker:v1`)
    - Reload restores list
    - Keep UI simple — one page, no router
  </action>
  <verify>Manual: add expense → reload → expense still present with correct category</verify>
  <done>AC-1 satisfied: categorized expenses persist across reload</done>
</task>

<task type="auto">
  <name>This-month totals chart</name>
  <files>src/chart.js, src/main.js, src/ui.js, index.html, package.json</files>
  <action>
    - Filter expenses to current calendar month
    - Aggregate totals by category
    - Render a chart (bar or doughnut) for this month
    - Show a simple text total for the month as well
    - Prefer Chart.js (npm or CDN) — one dependency max for charts
    - Re-render chart when expenses change
  </action>
  <verify>Manual: add 2+ expenses this month in different categories → chart updates; total matches sum</verify>
  <done>AC-2 satisfied: monthly category totals visible on chart</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/**` content except via PAUL workflows (APPLY may update STATE via unify later)
- Auth / user accounts
- Backend API, database, Laravel scaffold

## SCOPE LIMITS
- Single-page MVP only
- localStorage only (no sync, no multi-device)
- Categories: fixed starter list (editable later = out of scope)
</boundaries>

<verification>
- [ ] `npm run build` passes
- [ ] Add expense with category → appears in list
- [ ] Reload → data persists
- [ ] Chart shows this month’s category totals
- [ ] No login UI present
</verification>

<success_criteria>
- [ ] AC-1, AC-2, AC-3 met
- [ ] README documents run steps
- [ ] App usable without network after first install (CDN exception OK if Chart.js via CDN)
</success_criteria>

<output>
After completion, create SUMMARY.md at:
`examples/expense-tracker/.paul/phases/01-mvp-happy-path/01-01-SUMMARY.md`
</output>
